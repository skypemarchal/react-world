export const GET_USER_PSEUDO = "GET_USER_PSEUDO"

export const getUserPseudo = (pseudo) => {
    return (dispatch) => {

        //const user = 

        dispatch({type: GET_USER_PSEUDO, payload: {id: 1, pseudo: "miche"}})
    }
}